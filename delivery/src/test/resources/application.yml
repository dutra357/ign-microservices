spring:
  application:
    name: delivery-tracking
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres_test
    username: postgres
    password: postgres